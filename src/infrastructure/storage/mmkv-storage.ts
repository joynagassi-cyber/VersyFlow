/**
 * @deprecated Use LocalStorageAdapter from './local-storage' instead
 * MMKV was never actually installed; this adapter uses localStorage
 */
export { LocalStorageAdapter as MmkvStorage } from './local-storage';
export type { LocalStorageAdapter as IMmkvStorage } from './local-storage';
